package hu.poketerkep.client.map.scanner;


import org.junit.Test;

public class MapScannerInstanceTest {

    @Test
    public void test() {
//        //Mock ClientService
//        ClientService clientService = Mockito.mock(ClientService.class);
//        Mockito.when(clientService.nextScanLocation()).thenReturn(Optional.of(Coordinate.fromDegrees(47.1, 19.2)));
//
//        //Mock UserService
//        UserService userService = Mockito.mock(UserService.class);
//        Mockito.when(userService.nextUser())
//                .thenReturn(Optional.of(RandomUserConfigGenerator.generateWorking()));
//
//        //Mock MapManager
//        MapManager mapManager = Mockito.mock(MapManager.class);
//        Mockito.when(mapManager.getClientService()).thenReturn(clientService);
//        Mockito.when(mapManager.getUserService()).thenReturn(userService);
//        Mockito.when(mapManager.getProxy()).thenReturn(Proxy.NO_PROXY);
//
//        MapScannerInstance instance = new MapScannerInstance(1, mapManager);


        //TODO

    }

}